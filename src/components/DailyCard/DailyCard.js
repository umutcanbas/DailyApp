import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const DailyCard = () => {
  return (
    <View>
      <Text style={{color:'white'}}>DailyCard</Text>
    </View>
  )
}

export default DailyCard

const styles = StyleSheet.create({})